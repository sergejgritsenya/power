import { Card, CardContent } from "@material-ui/core"
import React, { FC } from "react"

export const AdminList: FC = () => {
  return (
    <Card>
      <CardContent>
        <h1>Admin List</h1>
      </CardContent>
    </Card>
  )
}
